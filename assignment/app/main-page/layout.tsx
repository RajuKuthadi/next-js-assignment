'use client'
import React from 'react'

export default function MainPageLayout({children}:{children: React.ReactNode}) {
  return (
    <div>
        <div>test</div>
        {children}
    </div>
  )
}
